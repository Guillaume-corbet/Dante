/*
** EPITECH PROJECT, 2018
** str_isneg
** File description:
** returns 1 if pos, 0 if neg
*/

#include "my.h"

int my_isneg(int n)
{
    if (n < 0)
        return (0);
    else
        return (1);
}
